server:
  port: 9090
  address: localhost

logging:
  level:
    edu.obya.blueprint: info
    org.flywaydb: info
    org.springframework: info
#    io.r2dbc.postgresql.QUERY: DEBUG # for queries
#    io.r2dbc.postgresql.PARAM: DEBUG # for parameters

zonky:
  test:
    database:
      provider: DOCKER
      type: POSTGRES
